# 第 32 章 数值域与数值半径

<div class="context-flow" markdown>

**前置**：内积空间 (Ch08) · 特征值 (Ch06) · 凸集基础 (Ch25)

**本章脉络**：数值域 (Numerical Range) 定义 $\to$ Toeplitz-Hausdorff 凸性定理 $\to$ 谱包含性质 $\to$ 数值半径 (Numerical Radius) $w(A)$ $\to$ 数值半径不等式（与谱范数的关系） $\to$ 2 阶矩阵的椭圆定理 $\to$ 正常阵的数值域（凸包） $\to$ 算子稳定性中的应用

**延伸**：数值域是特征值在复平面上的“连续扩张”；它在分析非正常算子的瞬态增长、克雷洛夫子空间方法的收敛性以及量子可观测量的取值范围中具有关键作用

</div>

如果特征值 $\sigma(A)$ 是矩阵在复平面上的“离散指纹”，那么**数值域** $W(A)$ 就是它的“连续阴影”。数值域捕捉了矩阵作为二次型在单位球上的全部取值可能。对于正常矩阵，这个阴影只是特征值的凸包；但对于非正常矩阵，数值域展现出极其复杂的几何形态，揭示了算子背后的不稳定性。

---

## 32.1 数值域的定义与凸性

!!! definition "定义 32.1 (数值域 / 算子值域)"
    方阵 $A \in M_n(\mathbb{C})$ 的**数值域**定义为单位球面上二次型的取值集合：
    $$W(A) = \{ \mathbf{x}^* A \mathbf{x} : \mathbf{x} \in \mathbb{C}^n, \|\mathbf{x}\| = 1 \}$$

!!! theorem "定理 32.1 (Toeplitz-Hausdorff 凸性定理)"
    对于任何矩阵 $A$，其数值域 $W(A)$ 总是复平面上的一个**凸集**。
    **意义**：尽管单位球面是弯曲的，但二次型的映射结果在复平面上却呈现出完美的凸性。

---

## 32.2 谱包含与正常阵

!!! theorem "定理 32.2 (谱包含性质)"
    矩阵 $A$ 的所有特征值都包含在其数值域中：$\sigma(A) \subseteq W(A)$。
    此外，$W(A)$ 还包含 $A$ 的凸包 $\operatorname{conv}(\sigma(A))$。

!!! theorem "定理 32.3 (正常阵的数值域)"
    $A$ 是正常矩阵 ($AA^* = A^*A$) $\iff$ $W(A) = \operatorname{conv}(\sigma(A))$。
    对于非正常矩阵，$W(A)$ 通常严格大于特征值的凸包。

---

## 32.3 数值半径 $w(A)$

!!! definition "定义 32.2 (数值半径)"
    数值域中元素模的最大值称为**数值半径**：
    $$w(A) = \sup \{ |z| : z \in W(A) \}$$

!!! theorem "定理 32.4 (数值半径不等式)"
    数值半径与谱范数 $\|A\|_2$ 满足以下双边不等式：
    $$\frac{1}{2} \|A\|_2 \le w(A) \le \|A\|_2$$
    这说明 $w(A)$ 与算子范数是等价的，但在研究幂收敛性时，$w(A)$ 往往提供更细致的刻画。

---

## 练习题

1. **[基础] 计算单位矩阵 $I$ 的数值域。**

   ??? success "参考答案"
       $x^* I x = x^* x = 1$（对所有 $\|x\|=1$）。故 $W(I) = \{1\}$，即复平面上的一个点。

2. **[对角阵] 求 $A = \operatorname{diag}(1, i)$ 的数值域。**

   ??? success "参考答案"
       由于 $A$ 是正常阵，$W(A)$ 是特征值 1 和 $i$ 的凸包，即连接 1 和 $i$ 的线段。

3. **[凸性] 举出一个例子说明 $\sigma(A)$ 本身不是凸的。**

   ??? success "参考答案"
       $A = \operatorname{diag}(1, -1)$。谱为 $\{1, -1\}$，不包含 0。但 $W(A) = [-1, 1]$ 包含 0。

4. **[数值半径] 求 $A = \begin{pmatrix} 0 & 1 \\ 0 & 0 \end{pmatrix}$ 的数值域。**

   ??? success "参考答案"
       由 2 阶矩阵的椭圆定理，其数值域是以原点为中心、半径为 0.5 的圆盘（不含边界？不，含边界的闭圆盘）。

5. **[性质] 证明 $W(A+cI) = W(A) + c$。**

   ??? success "参考答案"
       $x^*(A+cI)x = x^*Ax + c(x^*x) = x^*Ax + c$。

6. **[自伴] 证明：若 $A$ 是 Hermite 阵，则 $W(A)$ 是实轴上的区间 $[\lambda_{\min}, \lambda_{\max}]$。**

   ??? success "参考答案"
       Hermite 阵的二次型 $x^*Ax$ 总是实数，且由 Rayleigh 商性质，其取值范围恰为特征值的最小到最大区间。

7. **[范数] 已知 $\|A\|_2 = 10$，数值半径 $w(A)$ 的最小可能值是多少？**

   ??? success "参考答案"
       由不等式 $w(A) \ge \frac{1}{2}\|A\|_2$，得 $w(A) \ge 5$。

8. **[迹] 数值域的质心与矩阵的迹有什么关系？**

   ??? success "参考答案"
       对于 $n$ 阶矩阵，其数值域包含点 $\frac{1}{n} \operatorname{tr}(A)$（所有基向量对应的二次型均值的平均）。

9. **[酉不变] 证明 $W(U^* A U) = W(A)$ 对任何酉矩阵 $U$ 成立。**

   ??? success "参考答案"
       $(Ux)^* A (Ux) = y^* A y$，其中 $y=Ux$。由于 $U$ 是酉变换，它将单位球面映为自身，故取值范围不变。

10. **[应用] 为什么数值域在非正常算子的稳定性分析中比特征值更可靠？**

   ??? success "参考答案"
        特征值只决定长期（渐近）稳定性，而数值域的大小限制了短期内（瞬态）解的范数增长。即使特征值都在左半平面，若数值域伸入右半平面，系统仍可能经历剧烈的瞬态波动。

## 本章小结

数值域是矩阵谱论的几何拓广：

1.  **形态的凸性**：Toeplitz-Hausdorff 定理揭示了线性算子作用下的一种深刻一致性，即无论算子如何复杂，其“投影”总是复平面上的凸区域。
2.  **正常性的分水岭**：数值域与特征值凸包的重合程度是衡量矩阵“正常性”（即是否具有标准正交特征基）的直观几何标准。
3.  **解析的界限**：数值半径确立了算子大小的新测度，为非正常系统的瞬态分析和迭代收敛速度提供了比谱半径更紧致的上限。
