# 第 14 章 矩阵分析

<div class="context-flow" markdown>

**前置**：矩阵函数 (Ch13) · 微积分级数理论 · 范数基础 (Ch15)

**本章脉络**：从标量分析到矩阵分析 $\to$ 矩阵序列及其收敛性 $\to$ 矩阵级数的收敛性 $\to$ 谱半径 $\rho(A)$ 的核心作用 $\to$ Gelfand 公式 $\to$ 矩阵幂 $A^k$ 的收敛准则 $\to$ Gershgorin 圆盘定理 $\to$ 应用：线性方程组的迭代解法（Jacobi, Gauss-Seidel）

**延伸**：矩阵分析是数值分析与连续动力系统的交叉学科；它将微积分的连续工具引入了离散的矩阵世界，是分析大规模系统稳定性的关键

</div>

矩阵分析研究矩阵作为变量时的解析性质。核心问题在于定义什么是“矩阵的接近”，以及如何处理矩阵序列的长期演化。与初等代数不同，矩阵分析更关注极限、收敛速度以及谱的分布。

---

## 14.1 矩阵序列与收敛性

!!! definition "定义 14.1 (矩阵序列收敛)"
    矩阵序列 $\{A_k\}$ 收敛于 $A$，记作 $\lim_{k \to \infty} A_k = A$，若对于矩阵的每一个分量都有 $\lim_{k \to \infty} (A_k)_{ij} = a_{ij}$。
    **等价性**：这等价于对任何矩阵范数 $\|\cdot\|$，都有 $\lim_{k \to \infty} \|A_k - A\| = 0$。

---

## 14.2 谱半径与收敛准则

!!! definition "定义 14.2 (谱半径 $\rho(A)$)"
    方阵 $A$ 的**谱半径**是其所有特征值模的最大值：
    $$\rho(A) = \max \{|\lambda| : \lambda \in \sigma(A)\}$$

!!! theorem "定理 14.1 (幂收敛定理)"
    矩阵幂序列 $\lim_{k \to \infty} A^k = O$ 当且仅当 $\rho(A) < 1$。

!!! theorem "定理 14.2 (Gelfand 公式)"
    对于任何矩阵范数 $\|\cdot\|$，都有：
    $$\rho(A) = \lim_{k \to \infty} \|A^k\|^{1/k}$$
    这建立了算子的纯代数属性（谱半径）与解析属性（范数）之间的深刻联系。

---

## 14.3 Gershgorin 圆盘定理

!!! theorem "定理 14.3 (Gershgorin 圆盘定理)"
    矩阵 $A$ 的所有特征值都位于以下 $n$ 个圆盘的并集中：
    $$R_i = \left\{ z \in \mathbb{C} : |z - a_{ii}| \le \sum_{j \neq i} |a_{ij}| \right\}$$
    **应用**：这提供了一种无需计算特征值即可快速估计其大致分布范围的方法。

---

## 练习题

1. **[收敛判定] 设 $A = \begin{pmatrix} 0.5 & 1 \\ 0 & 0.5 \end{pmatrix}$。序列 $A^k$ 是否收敛于零矩阵？**
   ??? success "参考答案"
       是的。特征值为 0.5，谱半径 $\rho(A) = 0.5 < 1$。

2. **[级数] 给定 $A = \begin{pmatrix} 0.1 & 0 \\ 0 & 0.2 \end{pmatrix}$。计算级数 $\sum_{k=0}^\infty A^k$。**
   ??? success "参考答案"
       由于 $\rho(A) < 1$，级数收敛于 $(I-A)^{-1} = \begin{pmatrix} 1/0.9 & 0 \\ 0 & 1/0.8 \end{pmatrix} = \operatorname{diag}(1.11, 1.25)$。

3. **[谱半径] 举出一个矩阵 $A$，使得对某种范数有 $\|A\| > 1$ 但 $\rho(A) < 1$。**
   ??? success "参考答案"
       例如 $A = \begin{pmatrix} 0 & 10 \\ 0 & 0 \end{pmatrix}$。其特征值为 0，故 $\rho(A)=0 < 1$。但其 2-范数为 10。

4. **[圆盘定理] 对 $A = \begin{pmatrix} 4 & 1 \\ 1 & 3 \end{pmatrix}$ 使用圆盘定理估计特征值范围。**
   ??? success "参考答案"
       特征值位于以 4 为圆心半径 1 的圆和以 3 为圆心半径 1 的圆的并集中，即区域 $[2, 5]$。

5. **[导数初步] 计算矩阵 $A(t) = \begin{pmatrix} t & t^2 \\ 1 & e^t \end{pmatrix}$ 的导数 $A'(t)$。**
   ??? success "参考答案"
       $A'(t) = \begin{pmatrix} 1 & 2t \\ 0 & e^t \end{pmatrix}$。

6. **[积分] 计算 $\int_0^1 \begin{pmatrix} x & 1 \\ 0 & 3x^2 \end{pmatrix} dx$。**
   ??? success "参考答案"
       $\begin{pmatrix} 1/2 & 1 \\ 0 & 1 \end{pmatrix}$。

7. **[Gelfand应用] 证明对于任何诱导范数，都有 $\rho(A) \le \|A\|$。**
   ??? success "参考答案"
       设 $Ax = \lambda x$，则 $\|\lambda x\| = | \lambda | \|x\| = \|Ax\| \le \|A\|\|x\|$。由于 $x \neq 0$，得 $|\lambda| \le \|A\|$。

8. **[逆矩阵导数] 利用 $A(t)A^{-1}(t) = I$ 证明：$(A^{-1})' = -A^{-1} A' A^{-1}$。**
   ??? success "参考答案"
       对两边求导：$A' A^{-1} + A (A^{-1})' = 0 \implies A (A^{-1})' = -A' A^{-1}$。左乘 $A^{-1}$ 即得。

9. **[迹导数] 证明 $\frac{d}{dt} \operatorname{tr}(A(t)) = \operatorname{tr}(A'(t))$。**
   ??? success "参考答案"
       迹是线性组合，求导与求和可以交换。

10. **[迭代] 为什么在研究迭代法 $x_{k+1} = Bx_k + f$ 时，矩阵 $B$ 的谱半径至关重要？**
    ??? success "参考答案"
        误差演化遵循 $e_{k+1} = B e_k$。误差消失（收敛）的充要条件是 $B^k \to 0$，由矩阵分析可知这等价于 $\rho(B) < 1$。

## 本章小结

矩阵分析为静态的代数赋予了时间的维度：

1.  **收敛准则**：谱半径是不变演化的终极裁判，它决定了离散动力系统是趋于稳定还是走向爆炸。
2.  **分析工具**：矩阵微积分为优化算法和变分问题提供了符号引擎，使得我们能对复杂的矩阵模型进行动态分析。
3.  **全局视角**：Gelfand 公式和圆盘定理将局部的元素信息与全局的谱属性完美统一，为数值稳定性预估提供了便捷路径。
